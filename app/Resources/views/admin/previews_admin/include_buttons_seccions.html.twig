<div class="buttons_sections">
    <ul>
        {% if seccions['wedding'] == true %}
            {% if active_sections == 'wedding' %}
                <li>
                    <a class="btn btn-default active" href="{{ path('admin_negocio_preview_seccion', {'slug_site': "wedding",'id':id}) }}">viendo como Wedding</a>
                </li>
            {% else %}
                <li>
                    <a class="btn btn-default" href="{{ path('admin_negocio_preview_seccion', {'slug_site': "wedding",'id':id}) }}">ver como Wedding</a>
                </li>
            {% endif %}
        {% endif %}
        {% if seccions['kids'] == true %}
            {% if active_sections == 'kids' %}
                <li>
                    <a class="btn btn-default active" href="{{ path('admin_negocio_preview_seccion', {'slug_site': "kids",'id':id}) }}">viendo como Kids</a>
                </li>
            {% else %}
                <li>
                    <a class="btn btn-default" href="{{ path('admin_negocio_preview_seccion', {'slug_site': "kids",'id':id}) }}">ver como Kids</a>
                </li>
            {% endif %}

        {% endif %}
        {% if seccions['party'] == true %}
            {% if active_sections == 'party' %}
                <li>
                    <a class="btn btn-default active" href="{{ path('admin_negocio_preview_seccion', {'slug_site': "party",'id':id}) }}">viendo como Party</a>
                </li>
            {% else %}
                <li>
                    <a class="btn btn-default" href="{{ path('admin_negocio_preview_seccion', {'slug_site': "party",'id':id}) }}">ver como Party</a>
                </li>
            {% endif %}

        {% endif %}
        {% if seccions['dinner'] == true %}
            {% if active_sections == "dinner" %}
                <li>
                    <a class="btn btn-default active" href="{{ path('admin_negocio_preview_seccion', {'slug_site': "dinner",'id':id}) }}">viendo como Dinner</a>
                </li>
            {% else %}
                <li>
                    <a class="btn btn-default" href="{{ path('admin_negocio_preview_seccion', {'slug_site': "dinner",'id':id}) }}">ver como Dinner</a>
                </li>
            {% endif %}

        {% endif %}
    </ul>
</div>

<hr>