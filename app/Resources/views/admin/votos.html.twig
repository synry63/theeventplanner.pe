<html>

<head>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href='https://fonts.googleapis.com/css?family=Lato:400,300italic,100italic,700,700italic,400italic,300,100' rel='stylesheet' type='text/css'>
    <link rel="stylesheet" href="{{ asset('css/bootstrap.min.css') }}">
    <link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/font-awesome/4.6.1/css/font-awesome.min.css">
    <link rel="stylesheet" href="{{ asset('css/styles.css') }}">
    <title>Dashboard</title>
</head>

<body>
<!--
        <nav id="nav-dashboard" class="navbar navbar-default">
            {{ knp_menu_render('AppBundle:Builder:menuAdmin',{ 'template': 'knp_menu.html.twig'}) }}
        </nav>
        -->

{{ include('admin/includes/menu.html.twig', { 'seccions': seccion }) }}

<div class="container">
    <div class="col-md-10 body-dashboard" style="margin-top: 30px">
        {% for label, flash in app.session.flashbag.get('success') %}
            <div class="alert alert-success">
                {{ flash }}
            </div>
        {% endfor %}

        <section id="demo">
            {% for v in votos  %}
                <article class="white-panel wedding_link">
                    <a onclick="return confirm('¿Estas seguro de eliminar este voto?');" class="admin_delete" href="{{ path('admin_voto_delete', {'id': v.id}) }}"><i class="fa fa-times" aria-hidden="true"></i></a>
                    <a class="admin_edit" href="{{ path('admin_voto_edit', {'id': v.id}) }}"><i class="fa fa-pencil-square-o" aria-hidden="true"></i> Editar</a>
                    <a href="{{ path('admin_tendencia_inspiraciones', {'id': v.id}) }}">
                        <h1><a href="javascript:void(0)">{{ v.nombre }}</a></h1>
                        <p>"{{ v.description }}"</p>
                    </a>
                </article>
            {% endfor %}

        </section>

    </div>
</div>






<script type="text/javascript" src="{{ asset('js/jquery-1.11.0.min.js') }}"></script>
<script type="text/javascript" src="{{ asset('js/bootstrap.js') }}"></script>
<script type="text/javascript" src="{{ asset('js/pinterest_grid.js') }}"></script>

<script>

    $('#demo').pinterest_grid({
        no_columns: 3,
        padding_x: 10,
        padding_y: 10,
        margin_bottom: 50,
        single_column_breakpoint: 700
    });

</script>
</body>

</html>













