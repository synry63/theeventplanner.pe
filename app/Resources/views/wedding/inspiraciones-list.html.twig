{% extends 'FOSUserBundle::layout.html.twig' %}

{% block content %}
    {{ parent() }}
    <h1>fdfsd</h1>
    {{ dump(inspiraciones) }}

    <div class="navigation">
        {{ knp_pagination_render(inspiraciones) }}
    </div>
{% endblock %}